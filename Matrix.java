package alpha;

public class Matrix {
	int m, n; //m rows, n columns 
	/*
	 * int rows, columns?
	 * 
	 * m (as slope) is a vector(run, rise)
	 * b (as y-intercept) is altering j
	 * 
	 * mx + by = c 
	 * m = (c - by)/x
	 * 
	 * y = mx + b
	 * m = (y - b)/x
	 * 
	 * two points: origin(0,0) and point(x, y)
	 * 
	 * =[1, 20; 9, 5; -13, -6]
	 * = x1 + 20x2
	 * 
	 * implement: addition, scalar multiplication, transposition
	 */
	// int rows, columns?
}
