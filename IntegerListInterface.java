package alpha;

public interface IntegerListInterface {
	int getAt(int index);
	void append(int toAdd);
	void removeAt(int index);
	void insertAt(int toAdd, int index);
	// fundamental methods for a list 
}
